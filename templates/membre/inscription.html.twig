{% extends "layout.html.twig" %}

{% set active = 'inscription' %}

{% block page_title %}
    S'inscrire
{% endblock %}

{% block content %}

    <div class="container">
        <div class=" sign-up row justify-content-center">
            <div class="col-8">
                <h2 class="text-center text-warning">
                    Inscription
                </h2>
                {{ form(form, {attr:{novalidate: 'novalidate' }}) }}
            </div>
        </div>
    </div>
{% endblock %}
